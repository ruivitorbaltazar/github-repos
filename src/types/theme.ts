type ThemeMode = "light" | "dark" | "system"

type Theme = {
  textPrimary: string
  textSecondary: string
  textTertiary: string
  bgPrimary: string
  bgSecondary: string
  bgTertiary: string
  shadowPrimary: string
}
export { ThemeMode, Theme }
